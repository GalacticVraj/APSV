# Build hosts set NODE_ENV=production, and npm then omits devDependencies —
# which on this repo means no vite and no typescript, so the build cannot run.
# This restores them no matter how the install is invoked, including when a
# deployment platform overrides the install command from its own dashboard.
include=dev
