{
  "name": "carbonloop",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/frontend",
    "packages/backend",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently -n frontend,backend -c green,blue \"npm run dev -w packages/frontend\" \"npm run dev -w packages/backend\"",
    "build": "npm run build -w packages/frontend && npm run build -w packages/backend",
    "test": "npm run test -w packages/backend && npm run test -w packages/frontend",
    "seed": "npm run seed -w packages/backend",
    "migrate": "npm run migrate -w packages/backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
